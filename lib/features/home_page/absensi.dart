import 'package:flutter/material.dart';

class AbsenScreen extends StatelessWidget {
  final String kelas;
  const AbsenScreen({super.key, required this.kelas});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text(' 11rpl')),
      body: const Center(),
    );
  }
}
